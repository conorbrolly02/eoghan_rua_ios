
[project]
name = "eoghan_rua_team_selector"
version = "1.0.0"
description = "Offline team selector app for Eoghan Rua GAA."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Eoghan Rua GAA", email = "info@eoghanrua.ie" }]
license = { file = "LICENSE" }   # PEP 621 format; remove any old `license = "Proprietary"`

# Optional but recommended fields
homepage = "https://eoghanrua.ie"

[tool.briefcase]
project_name = "Eoghan Rua Team Selector"
bundle = "ie.eoghanrua"
url = "https://eoghanrua.ie"
sources = ["eoghan_rua_team_selector"]

[tool.briefcase.app.eoghan_rua_team_selector]
formal_name = "Eoghan Rua Team Selector"
description = "Offline team selector app for Eoghan Rua GAA."
app_name = "eoghan_rua_team_selector"

# 👇 Your iOS entry point (contains EoghanRuaApp + main())
module = "eoghan_rua_team_selector.app"

# 👇 Branding assets in your repo
icon   = "assets/icon.png"
splash = "assets/splash.png"

# 👇 Files you load at runtime with: Path(app.paths.app) / "resources" / "crest.png"
resources = ["assets/crest.png"]

# 👇 App dependencies (single, merged list — no duplicates)
#    - std-nslog routes Python prints/tracebacks to iOS NSLog (so you’ll see them in idevicesyslog)
#    - Pillow and XlsxWriter match what you installed earlier
#    - toga-ios pulls in the correct toga/rubicon versions for iOS
requires = [
    "toga-ios>=0.4.7",
    "std-nslog>=1.0.0",
    "Pillow>=10.0.0",
    "XlsxWriter>=3.1.9"
]

# (Optional) If you later need platform-specific customizations, you can add:
# [tool.briefcase.app.eoghan_rua_team_selector.iOS]
#   # iOS-specific settings go here
